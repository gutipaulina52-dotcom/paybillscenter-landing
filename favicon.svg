"use client";
import { Phone, Mail, ArrowUp, Facebook, Instagram } from "lucide-react";

const services = ["Electricity", "Water", "Gas", "Internet", "Phone", "Rent"];
const links = [
  { label: "Services", href: "#services" },
  { label: "Why Us", href: "#why-choose-us" },
  { label: "How It Works", href: "#how-it-works" },
  { label: "Pricing", href: "#pricing" },
  { label: "Contact", href: "#contact" },
];

export default function Footer() {
  const scrollToTop = () => window.scrollTo({ top: 0, behavior: "smooth" });
  const handleNav = (e: React.MouseEvent<HTMLAnchorElement>, href: string) => {
    e.preventDefault();
    document.querySelector(href)?.scrollIntoView({ behavior: "smooth" });
  };

  return (
    <footer className="bg-[#0D1F3C] text-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 py-16">
        <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-10 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-3 mb-5">
              <div className="w-11 h-11 rounded-2xl bg-gradient-to-br from-[#1B3A6B] to-[#2E5FA1] flex items-center justify-center shadow-lg">
                <span className="text-white font-black text-lg">B</span>
              </div>
              <div>
                <span className="font-display font-bold text-white text-base block leading-tight">Bills Solutions</span>
                <span className="text-[#C41E3A] text-[10px] font-bold tracking-[0.2em] uppercase">Center</span>
              </div>
            </div>
            <p className="text-white/40 text-sm leading-relaxed mb-6">
              Professional bill payment services for busy Americans. Fast. Secure. Reliable.
            </p>
            <p className="text-white/30 text-xs italic">"We Pay Bills, You Gain Peace of Mind"</p>
          </div>

          {/* Services */}
          <div>
            <h4 className="font-display font-bold text-white text-sm tracking-widest uppercase mb-5">Services</h4>
            <ul className="space-y-2.5">
              {services.map((s) => (
                <li key={s}>
                  <a href={`https://wa.me/16892985282?text=Hello%2C%20I%20need%20to%20pay%20my%20${encodeURIComponent(s)}%20bill`}
                    target="_blank" rel="noopener noreferrer"
                    className="text-white/40 hover:text-[#22C55E] text-sm transition-colors flex items-center gap-2">
                    <span className="w-1 h-1 rounded-full bg-[#22C55E] opacity-60" /> {s}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          {/* Quick Links */}
          <div>
            <h4 className="font-display font-bold text-white text-sm tracking-widest uppercase mb-5">Quick Links</h4>
            <ul className="space-y-2.5">
              {links.map((l) => (
                <li key={l.href}>
                  <a href={l.href} onClick={(e) => handleNav(e as any, l.href)}
                    className="text-white/40 hover:text-white text-sm transition-colors flex items-center gap-2">
                    <span className="w-1 h-1 rounded-full bg-white/30" /> {l.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          {/* Contact */}
          <div>
            <h4 className="font-display font-bold text-white text-sm tracking-widest uppercase mb-5">Contact</h4>
            <div className="space-y-4">
              <a href="https://wa.me/16892985282" target="_blank" rel="noopener noreferrer"
                className="flex items-center gap-3 text-white/40 hover:text-[#22C55E] transition-colors group">
                <div className="w-9 h-9 rounded-xl bg-white/5 group-hover:bg-[#22C55E]/10 flex items-center justify-center transition-colors">
                  <Phone className="w-4 h-4" />
                </div>
                <span className="text-sm">+1 (689) 298-5282</span>
              </a>
              <a href="mailto:billserviceamerican@gmail.com"
                className="flex items-center gap-3 text-white/40 hover:text-[#1B3A6B] transition-colors group">
                <div className="w-9 h-9 rounded-xl bg-white/5 group-hover:bg-[#1B3A6B]/20 flex items-center justify-center transition-colors">
                  <Mail className="w-4 h-4" />
                </div>
                <span className="text-sm">billserviceamerican@gmail.com</span>
              </a>
            </div>

            {/* Social */}
            <div className="flex gap-3 mt-6">
              <a href="https://facebook.com" target="_blank" rel="noopener noreferrer"
                className="w-10 h-10 rounded-xl bg-white/5 hover:bg-[#3B82F6]/20 flex items-center justify-center transition-all hover:scale-110">
                <Facebook className="w-4 h-4 text-white/40 hover:text-[#3B82F6]" />
              </a>
              <a href="https://wa.me/16892985282" target="_blank" rel="noopener noreferrer"
                className="w-10 h-10 rounded-xl bg-white/5 hover:bg-[#22C55E]/20 flex items-center justify-center transition-all hover:scale-110">
                <Phone className="w-4 h-4 text-white/40 hover:text-[#22C55E]" />
              </a>
            </div>
          </div>
        </div>

        {/* Bottom bar */}
        <div className="pt-8 border-t border-white/10 flex flex-col sm:flex-row items-center justify-between gap-4">
          <p className="text-white/25 text-xs">© 2026 Bills Solutions Center. All rights reserved. · Fast · Secure · Reliable</p>
          <button onClick={scrollToTop}
            className="w-10 h-10 rounded-xl bg-white/5 hover:bg-white/10 flex items-center justify-center transition-all hover:scale-110" aria-label="Back to top">
            <ArrowUp className="w-4 h-4 text-white/40" />
          </button>
        </div>
      </div>
    </footer>
  );
}
